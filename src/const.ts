import { resolve } from 'node:path';

export const root = resolve(__dirname, '..');
export const pluginDir = resolve(root, './public');
export const BASE_URL = 'https://f58637547.github.io/manifest/public/index.json';
